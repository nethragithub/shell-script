git config --global user.email "nethra.sangeetham@gmail.com"
git config --global user.name "nethragithub"
git config --global credential.helper store
git add . ; git commit -m "initial commit" ; git push -u origin main